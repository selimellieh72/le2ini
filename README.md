Steps to run:
1. ```
    docker compose build
2. ```
    docker compose up
    ```
3. ```
    ./scripts.sh migrate
    ```
4. ```
    ./scripts.sh createsuperuser
    ```
5. 
```
    ./scripts.sh seed
```